[project]
name = "yandex-wiki-search-mcp"
version = "1.3.0"
description = "MCP server for Yandex Wiki with full-text search"
readme = "README.md"
requires-python = ">=3.11"
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Typing :: Typed",
]
authors = [
    { name = "David Bolshov", email = "dlbolshov@gmail.com" },
    { name = "Aleksandr Ponkratov", email = "aleks.ponkratov@gmail.com" },
]
maintainers = [
    { name = "David Bolshov", email = "dlbolshov@gmail.com" },
]
dependencies = [
    "aiocache[redis]>=0.12.3",
    "aiohttp>=3.11.18",
    "cryptography>=44.0",
    # Capped below 3 because a major bump of the SDK is a rewrite, not an
    # upgrade — 1.0.0 shipped without that bound and died the day mcp 2.0
    # landed. Within 2.x the range stays open: `uv.lock` is what pins the
    # Docker image, and freezing an exact version here would only strand
    # uvx/pip users on it.
    "mcp[cli]>=2,<3",
    "pydantic>=2.12.4",
    "pydantic-settings>=2.8.1",
    "yarl>=1.20.0",
]

[tool.uv]
package = true

[tool.setuptools.packages.find]
where = ["."]
include = ["mcp_wiki*"]

[tool.setuptools.package-data]
mcp_wiki = ["py.typed"]

[project.scripts]
yandex-wiki-search-mcp = "mcp_wiki.__main__:main"

[project.urls]
Homepage = "https://github.com/dlbolshov/yandex-wiki-search-mcp"
Repository = "https://github.com/dlbolshov/yandex-wiki-search-mcp"
Issues = "https://github.com/dlbolshov/yandex-wiki-search-mcp/issues"

[dependency-groups]
dev = [
    "aioresponses>=0.7.7",
    "fakeredis>=2.37.0",
    # starlette.testclient needs it, and mcp 2.x no longer pulls it in: the
    # SDK moved to httpx2. Ours to declare now.
    "httpx>=0.28",
    "mypy>=1.8.0",
    "pytest>=8.0.0",
    "pytest-asyncio>=0.24.0",
    "pytest-cov>=5.0.0",
    "pytest-mock>=3.14.0",
    "pytest-xdist>=3.5.0",
    "ruff>=0.8.0",
    "ty>=0.0.8",
]

[tool.mypy]
plugins = ["pydantic.mypy"]
python_version = "3.11"
# The logo generator is a vendored design artifact with its own compact
# style, not package code — every checker skips it (see also [tool.ruff]
# and [tool.ty.src]). `local/` is a gitignored scratch dir that only
# exists on dev machines; mypy is the one checker that doesn't respect
# .gitignore and trips over duplicate module names in there.
exclude = ["^docs/assets/logo/build\\.py$", "^local/"]
check_untyped_defs = true
disallow_untyped_defs = true
warn_redundant_casts = true
warn_unused_configs = true
enable_error_code = ["ignore-without-code"]

[[tool.mypy.overrides]]
module = ["aiocache.*", "aioresponses.*"]
ignore_missing_imports = true

[tool.ruff]
extend-exclude = ["docs/assets/logo/build.py"]

[tool.ty.src]
exclude = ["docs/assets/logo/build.py"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"

[tool.ruff.lint]
fixable = ["I", "RUF022", "RUF023", "F401"]
preview = true
select = [
    "E",
    "W",
    "F",
    "I",
    "C",
    "B",
    "T20",
    "UP",
    "SIM",
    "RUF",
    "PTH",
    "ASYNC",
    "PERF",
    "TRY",
    "S",
]
ignore = [
    "E501",
    "B008",
    "C901",
    "TRY003",
    "RUF029",
    "RUF067",
]

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["S101", "S104", "S105", "S106", "RUF001", "RUF002", "RUF003"]
"scripts/**" = ["T201", "RUF001", "RUF002", "RUF003"]

[tool.ruff.lint.isort]
combine-as-imports = true
extra-standard-library = ["typing_extensions"]
section-order = [
    "future",
    "standard-library",
    "third-party",
    "first-party",
    "local-folder",
]
no-lines-before = []

[tool.ruff.lint.pep8-naming]
classmethod-decorators = ["cached_classproperty", "classproperty"]

[tool.coverage.run]
branch = true
source = ["mcp_wiki"]

[tool.coverage.report]
# Extends coverage's defaults (which already honor `# pragma: no cover`).
exclude_also = [
    # Runs only when the module is executed as a script, which the suite
    # never does — it imports and calls main() directly.
    'if __name__ == "__main__":',
    "if TYPE_CHECKING:",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
addopts = [
    "-n",
    "auto",
    "--strict-markers",
    "--tb=short",
]
